## This program is made to be used with OBS and other top 8 visual representation softwares

Each "file" is made to be linked into an obs overlay, so that when updating the bracket from the app, it updates the overlay